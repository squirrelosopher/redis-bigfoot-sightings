export default class Constants {
    //////////////////////////////////////////////////////////////
    /////////////////////// Redis constants //////////////////////
    //////////////////////////////////////////////////////////////
    static REDIS_HOST = process.env.REDIS_HOST || '127.0.0.1'; 
    static REDIS_PORT = parseInt(process.env.REDIS_PORT || '6379');
    static REDIS_USER = process.env.REDIS_USER || 'default';
    static REDIS_PASSWORD = process.env.REDIS_PASSWORD || '';
    static REDIS_DB_INDEX = parseInt(process.env.REDIS_PORT || '0');

    //////////////////////////////////////////////////////////////
    ///////////////////////// Redis keys /////////////////////////
    //////////////////////////////////////////////////////////////
    static REDIS_SIGHTING_KEY = 'sighting'
    static REDIS_INDEX_KEY = 'sightings-index'

    //////////////////////////////////////////////////////////////
    ////////////////////// Redis info keys ///////////////////////
    //////////////////////////////////////////////////////////////

    // General information about the Redis server.
    static SERVER_INFO = 'server';
    // Memory consumption related information.
    static MEMPORY_INFO = 'memory';
    // Database related statistics.
    static KEY_SPACE_INFO = 'keyspace';

    //////////////////////////////////////////////////////////////
    /////////////////// Miscellaneous constants //////////////////
    //////////////////////////////////////////////////////////////
    static DATA_PATH = 'data/bfro_reports_geocoded.csv'
    static SERVER_PORT = parseInt(process.env.PORT || '8080')
}